{
  "name": "nvlabs-org-root",
  "private": true,
  "version": "0.1.101",
  "scripts": {
    "dev": "concurrently -n gateway,web -c blue,green \"pnpm dev:gateway\" \"pnpm dev:web\"",
    "dev:web": "pnpm --filter web dev",
    "dev:gateway": "pnpm --filter nvlabs-org dev",
    "start": "pnpm --filter web build && pnpm --filter nvlabs-org start",
    "build": "pnpm -r build",
    "build:release": "pnpm --filter web build && pnpm --filter nvlabs-org build",
    "publish:release": "pnpm run build:release && cd apps/gateway && npm publish",
    "lint": "pnpm -r lint",
    "dev:desktop": "pnpm --filter nvlabs-org-desktop dev",
    "build:desktop": "bash scripts/install-desktop.sh"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^5.9.3"
  }
}
